[nixpacks]
# Railway 使用 Nixpacks 來構建
NODE_VERSION = 22

# 安裝依賴
INSTALL_CMD = """npm install && cd client && npm install"""

# 構建前端
BUILD_CMD = """cd client && npm run build"""

# 啟動命令
START_CMD = """node server.js"""

# 靜態文件夾 - Railway 會把 client/dist 的內容放到 static 這個環境變量指向的位置
