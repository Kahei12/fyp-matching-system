{
  "name": "fyp-matching-system",
  "version": "1.0.0",
  "description": "FYP Matching System - Monorepo with Express backend and React frontend",
  "main": "server.js",
  "scripts": {
    "setup": "npm install && cd client && npm install",
    "build": "cd client && npm install && npm run build",
    "start": "node server.js",
    "server:dev": "nodemon server.js",
    "client": "cd client && npm run dev",
    "dev": "concurrently \"npm run server:dev\" \"npm run client\"",
    "postinstall": "cd client && npm install --include=dev && npx vite build"
  },
  "engines": {
    "node": ">=18.0.0"
  },
  "dependencies": {
    "bcryptjs": "^3.0.2",
    "cors": "^2.8.6",
    "dotenv": "^17.2.3",
    "express": "^5.1.0",
    "json2csv": "^6.0.0-alpha.2",
    "mongodb": "^7.0.0",
    "mongoose": "^9.1.3",
    "pdf-parse": "^1.1.1"
  },
  "devDependencies": {
    "concurrently": "^9.1.2",
    "nodemon": "^3.1.10"
  }
}
